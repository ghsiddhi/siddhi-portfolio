import { styled } from "@mui/material";

export const ProBox = styled("div")(() => ({
  backgroundColor: "#fff",
  borderRadius: "1.7rem",
  boxShadow: "0 0 10px rgba(0, 0, 0, .1)",
  display: "flex",
  flexDirection: "row",
  gap: "8rem",
  height: "40rem",
  padding: "2rem",
}));
